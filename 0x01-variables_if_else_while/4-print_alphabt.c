#include <stdio.h>
/**
 * main - is the place we are printing a formatted string to stdout
 *
 * Return: returns 0
 */
int main () {
	char ch=0;
	for(ch = 'a' ; ch <= 'z' ; ch++)
	{
		(if ch == e || ch == q)
			continue;
		putchar(ch);
	}

   putchar(10);

   return(0);
}
