#include "main.h"
/**
 * _islower - short description
 * @c: input character
 * Return: 0 if lower
 */
int _islower(int c)
{
	if (('a' <= num && 'z' >= num) &&
		('A' <= num && 'Z' >= num))
		return (1);
	else
		return (0);
}
