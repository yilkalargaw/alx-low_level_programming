#include "main.h"
/**
 * _isalpha - short description
 * @num: input character
 * Return: 1 if pos, -1 if neg else 0
 */
int _abs(int num)
{
	if (num > 0)
	{
		return (num);
	}
	else if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (0);
	}
}
